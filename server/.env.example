PORT=5000
# Get a free connection string from MongoDB Atlas (https://www.mongodb.com/cloud/atlas)
# Leave this unset to run the server in MOCK MODE with in-memory seed data (no DB needed).
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/naikfoods?retryWrites=true&w=majority
